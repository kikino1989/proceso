import { ChartizeDirective } from './chartize.directive';

describe('ChartizeDirective', () => {
  it('should create an instance', () => {
    const directive = new ChartizeDirective();
    expect(directive).toBeTruthy();
  });
});
